---
layout: base
permalink: tags/{{ tag }}/
renderData:
  title: Tagged “{{ tag }}”
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
    - links
---

<h1 class="page-title">Tagged “{{ tag }}”</h1>

{% set postslist = collections[ tag ] %}
{% include 'partials/postslist.njk' %}

<p class="c-more-tags">Bekijk <a href="{{ '/tags/' | url }}">alle tags</a>.</p>
